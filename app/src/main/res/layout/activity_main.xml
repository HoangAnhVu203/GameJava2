<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#192A3A"
    android:padding="24dp"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Choose Game"
        android:textColor="#FFFFFF"
        android:textSize="34sp"
        android:textStyle="bold"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="18dp"
        android:fillViewport="true"
        android:scrollbars="none"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/titleText">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <Button android:id="@+id/basketballButton" style="@style/GameMenuButton" android:text="Basketball" />
            <Button android:id="@+id/ballButton" style="@style/GameMenuButton" android:text="Ball" />
            <Button android:id="@+id/tapeItUpButton" style="@style/GameMenuButton" android:text="TapeItUp" />
            <Button android:id="@+id/seaCatcherButton" style="@style/GameMenuButton" android:text="Sea Catcher" />
            <Button android:id="@+id/ballBlastButton" style="@style/GameMenuButton" android:text="Ball Blast" />
            <Button android:id="@+id/superBallFallButton" style="@style/GameMenuButton" android:text="Super Ball Fall" />
            <Button android:id="@+id/tinyFishingButton" style="@style/GameMenuButton" android:text="Tiny Fishing" />
            <Button android:id="@+id/flipperDrunkButton" style="@style/GameMenuButton" android:text="Flipper Drunk" />
            <Button android:id="@+id/peglinkoButton" style="@style/GameMenuButton" android:text="Peglinko" />
            <Button android:id="@+id/rocketLaunchButton" style="@style/GameMenuButton" android:text="Rocket Launch" />
            <Button android:id="@+id/dunkALotButton" style="@style/GameMenuButton" android:text="Dunk A Lot" />
            <Button android:id="@+id/airHockeyButton" style="@style/GameMenuButton" android:text="Air Hockey" />
            <Button android:id="@+id/flappyDunkButton" style="@style/GameMenuButton" android:text="Flappy Dunk" />
            <Button android:id="@+id/asteroidButton" style="@style/GameMenuButton" android:layout_marginBottom="0dp" android:text="Asteroid" />

        </LinearLayout>

    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
